---
date: '2026-03-04'
project: 뇽죵이Agent
session: obsidian-rest-api-전환
status: saved
tags:
  - handoff
  - obsidian
  - rest-api
  - 완료
type: handoff
---
# 세션 핸드오프 — 2026-03-04

## 🎯 이번 세션 완료 사항

### 1. ObsidianStore REST API 전환 (핵심 수정)
- **문제**: 파일시스템 직접 쓰기 방식 → 경로 하드코딩 의존, 볼트 불일치 위험
- **해결**: `obsidian-store.ts` 전면 재작성 — `fetch(Obsidian Local REST API)` 방식으로 전환
- **ETag 캐싱** 유지 (304 Not Modified로 조건부 GET)
- **서버사이드 검색** 활용 (`POST /search/simple/`) + 로컬 스캔 fallback

### 2. 설정 변경 (settings.json)
```json
"OBSIDIAN_API_URL": "http://127.0.0.1:27123",
"OBSIDIAN_API_KEY": "176eab4a9d880276954d940b0bf681c4bcfaf2a2cbd409040fbbc0f6db14c2df",
"AGENT_DATA_DIR": "뇽죵이Agent"
```
- `OBSIDIAN_VAULT_PATH`, `AGENT_DATA_DIR` (절대경로) 완전 제거
- 이제 어떤 프로젝트/경로에서도 경로 설정 없이 동작

### 3. 영향받은 파일
- `src/core/obsidian-store.ts` — REST API 완전 전환
- `src/core/config.ts` — OBSIDIAN_API_URL / OBSIDIAN_API_KEY 추가, OBSIDIAN_VAULT_PATH 제거
- `src/mcp-server.ts` — ObsidianStore 생성자 변경
- `src/agent.ts` — ObsidianStore 생성자 변경
- `src/index.ts` — 로그 메시지 수정
- `src/dashboard/server.ts` — 헬스체크 REST API 방식으로 교체
- `C:\Users\AIcreator\.gemini\settings.json` — API Key/URL 적용

### 4. 테스트 재작성 (290/290 통과)
- `tests/core/obsidian-store.test.ts` → fetch mock 기반
- `tests/core/task-manager.test.ts` → in-memory fake store
- `tests/personas/persona.test.ts` → in-memory fake store

## 📦 Obsidian 플러그인 리서치 결과

### 설치 권장 (우선순위 순)
1. **QuickAdd** (9.6) — AI 호출 + 매크로 자동화
2. **Dataview** (9.0) — SQL로 뇽죵이 메모리 시각화
3. **Templater** (8.8) — 동적 템플릿 + JS 실행
4. **Obsidian Copilot** (9.2) — 볼트 전체 AI 채팅
5. **Smart Connections** (8.4) — 벡터 임베딩 유사 노트 연결
6. **Obsidian Git** (7.2) — 볼트 자동 백업

### AI 활용 전략
- memory_write 시 `status`, `project`, `type` frontmatter 표준화 → Dataview로 시각화
- Obsidian Git으로 매 memory_write마다 자동 커밋 → 이력 관리
- QuickAdd AI Assistant + Ollama 연동 → 대표님 원클릭 메모+요약

## 🔜 다음 세션 할 일
1. Reload Window 후 `agent_status` 호출 → `obsidianApi: http://127.0.0.1:27123` 확인
2. `memory_write` 테스트로 실제 Obsidian 저장 검증
3. Obsidian 플러그인 설치 (QuickAdd → Dataview → Templater 순)
4. Dataview 쿼리로 뇽죵이 메모리 시각화 대시보드 구성

## 🛑 주의사항
- Obsidian 앱이 꺼져 있으면 REST API 연결 안 됨 → memory_write 실패
- API Key는 settings.json에 평문 저장 — Git push 시 .gemini/ 디렉토리 제외할 것
- 테스트는 모두 mock 기반 → 실제 Obsidian 연동 E2E 테스트는 별도 작성 필요

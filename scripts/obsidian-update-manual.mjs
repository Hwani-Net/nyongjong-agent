const API_URL = 'http://127.0.0.1:27123';
const API_KEY = '176eab4a9d880276954d940b0bf681c4bcfaf2a2cbd409040fbbc0f6db14c2df';
const NOTE_PATH = '뇽죵이 설명서.md';

const newContent = `---
created: 2026-03-03
updated: 2026-03-07
pinned: true
tags:
  - 뇽죵이
  - 슬래시커맨드
  - 설명서
type: guide
version: 0.7.3
---

# 🐸 뇽죵이 설명서 — 슬래시 커맨드 가이드

> 채팅창에 **슬래시(\`/\`) + 커맨드**를 입력하면 뇽죵이가 자동으로 워크플로우를 실행합니다.
> 트리거 단어("분석", "자율" 등)만 입력해도 동일하게 동작합니다.
> 까먹었을 때: \`?\` 또는 \`도움\` 입력하면 전체 목록이 표시됩니다.

---

## 🚀 빠른 시작 — npm으로 설치/실행

\`\`\`bash
# MCP 서버 실행 (npx로 바로 실행)
npx nyongjong-agent

# 또는 전역 설치 후 실행
npm install -g nyongjong-agent
nyongjong-agent
\`\`\`

**현재 버전**: \`nyongjong-agent@0.7.3\` (npmjs.com/package/nyongjong-agent)

### Claude Desktop 설정 예시
\`\`\`json
{
  "mcpServers": {
    "nongjong-agent": {
      "command": "npx",
      "args": ["nyongjong-agent"],
      "env": {
        "OBSIDIAN_API_KEY": "...",
        "DASHBOARD_PORT": "3100"
      }
    }
  }
}
\`\`\`

---

## 📋 커맨드 목록

### 🔧 개발 작업

| 커맨드 | 언제 쓰나? | 뇽죵이가 하는 일 |
|--------|-----------|--------------------|
| \`/수정\` | 코드 고칠 때 | 코드 수정 → 빌드(tsc) → 테스트 → 품질 검사 → 결과 보고 |
| \`/디자인\` | UI 만들 때 | Stitch로 디자인 생성 → HTML 추출 → 코드 반영 → 빌드 |
| \`/기능\` | 기능 하나 추가할 때 | 목표 분석 → 전문가 자문 → 코딩 → 테스트 → 보고 |

### 💡 기획

| 커맨드 | 언제 쓰나? | 뇽죵이가 하는 일 |
|--------|-----------|------------------|
| \`/기획\` | 막연한 아이디어를 PRD로 만들 때 | 불명확 포인트 감지 → A/B/C 보기 질문 → PRD 초안 → 반복(최대 5회) → PRD 확정 |

### 📊 분석 & 전략

| 커맨드 | 언제 쓰나? | 뇽죵이가 하는 일 |
|--------|-----------|------------------|
| \`/분석\` | 복잡한 기능 구현할 때 | 목표 분석 → 전문가 자문 → 모델 추천 → 메모리 조회 → 코딩 |
| \`/조사\` | 기술/시장 조사할 때 | Tavily + search_web → 분석 보고서 작성 |
| \`/자문\` | 빠른 전문가 의견 필요할 때 | 페르소나 자문 단독 실행 → 의견 정리 |
| \`/발상\` | 킬러 피처 뽑을 때 | 경쟁사 리뷰 수집 → JTBD 핑퐁 → 10x 필터 → 킬러 피처 도출 |

### 🚀 프로젝트 관리

| 커맨드 | 언제 쓰나? | 뇽죵이가 하는 일 |
|--------|-----------|--------------------|
| \`/자율\` | 새 프로젝트 처음부터 끝까지 | PRD → Stitch 디자인 → 코딩 → 완료 보고 (기존 프로젝트 X) |
| \`/규칙\` | 뇽죵이가 삼천포 빠졌을 때 | 상태 확인 → 목표 재분석 → 품질 검사 → 워크플로우 복귀 |
| \`/셋업\` | 프로젝트 시작할 때 | PROJECT_CONTEXT.md + PITFALLS.md 자동 생성 |

### 🔁 반복 & 메모리

| 커맨드 | 언제 쓰나? | 뇽죵이가 하는 일 |
|--------|-----------|------------------|
| \`/루프\` | 성공할 때까지 반복할 때 | 지정 명령 N회 반복, 성공 조건 충족 시 자동 중단 |
| \`/이어서\` | 이전 대화 이어갈 때 | PROJECT_CONTEXT → PITFALLS → 메모리 조회 → 맥락 복구 |
| \`/저장\` | 지금 상태 저장할 때 | Obsidian에 현재 진행 상태 즉시 저장 |
| \`/메모리\` | 과거 기록 찾을 때 | 메모리 검색 → 결과 보고 |

---

## 🎯 상황별 추천 가이드

\`\`\`
"이 버그 좀 고쳐줘"          → /수정
"새 화면 만들어줘"           → /디자인
"○○ 만들어줘" (막연)         → /기획
"로그인 기능 추가해줘"       → /기능
"결제 시스템 설계해줘"       → /분석
"이 기술 좋은지 알아봐줘"    → /조사
"경쟁사 대비 뭐가 부족해?"   → /발상
"새 앱 처음부터 만들어줘"    → /자율
"어제 하던 거 이어서"        → /이어서
"지금 저장해둬"              → /저장
"테스트 통과할 때까지 돌려"   → /루프
\`\`\`

---

## 🔬 Skills 2.0 — 스킬 생애주기 관리 (v0.7.x)

> v0.7.x에서 추가된 MCP 도구 2종. 뇽죵이가 스스로 스킬을 감사하고 A/B 비교합니다.

### 핵심 도구

| 도구 | 역할 |
|------|------|
| \`skill_audit\` | .agent/skills/ 전체 스캔 → capability/workflow 분류 → 은퇴 후보 식별 |
| \`skill_benchmark\` | 스킬 ON/OFF A/B 비교 → KEEP/REVIEW/RETIRE 자동 판정 |

### 스킬 분류 기준 (ADR-005)

| 분류 | 설명 | 은퇴 가능? |
|------|------|-----------|
| ⚡ capability | 모델 한계 보완용 (API 래퍼, 포맷 변환) | ✅ 모델 발전 시 자동 은퇴 후보 |
| 🔧 workflow | 팀 규칙/배포 흐름/코딩 표준 | ❌ 영구 유지 |

### Eval 전수 검사 결과 (2026-03-07 최종)
\`\`\`
✅ KEEP   : 52개
🔴 RETIRE : 0개
🟡 REVIEW : 0개
\`\`\`

### 스킬 SKILL.md에 카테고리 추가 방법
\`\`\`yaml
---
name: my-skill
description: "스킬 설명"
category: capability  # 또는 workflow
---
\`\`\`

---

## 😤 김짜증 핑퐁 (UX 검증용 극단적 페르소나)

> 김짜증 = "짜증 폭발한 극단적 사용자" 페르소나. 기능의 UX를 극한까지 테스트할 때 사용.

| 목적 | 커맨드 |
|------|--------|
| 킬러 피처 전체 5단계 | \`/발상\` |
| 특정 기능 UX만 빠르게 검증 | \`/자문\` + "김짜증" 언급 |

### 과거 성과
- 혜택알리미 킬러 피처 3개 도출 (2026-03-03)
- "시스템 재알림 = 스팸" 발견 → 알림 재발송 금지 결정
- "프로필 한 번에 다 받으면 이탈" → 단계별 입력으로 변경

---

## 📦 버전 히스토리

| 버전 | 날짜 | 주요 변경 |
|------|------|----------|
| \`0.7.3\` | 2026-03-07 | 🔬 Eval Framework 완성 (KEEP:52/RETIRE:0) + SSE 버그 수정 + npm 배포 |
| \`0.7.1\` | 2026-03-06 | 🔍 Skills 2.0 — SkillLifecycleManager + A/B Benchmarking |
| \`0.6.0\` | 2026-03-03 | 🎨 Stitch 도구 3종 + npm 최초 배포 |
| \`0.5.0\` | 2026-02-27 | 🔥 Stage-Gate + Dashboard Health Checks |
| \`0.4.0\` | 2026-02-27 | 🏆 Ralph Mode + Grounding 6종 어댑터 |

---

## 📌 완료 시 보고 포맷

\`\`\`
## 📋 다음 작업 추천
| 항목 | 내용 |
|------|------|
| 모델명 | Gemini 3.1 Pro / Gemini 3 Flash / Claude Sonnet 4.6 |
| 모드 | Planning / Fast |
| 진행도 | 분석→설계→구현→검증→배포→완료 (현재 단계 볼드) |
| 컨텍스트 | 🟢 양호 / 🟡 잘림 / 🔴 새 세션 권장 |
| 추천 커맨드 | /기능, /수정, /디자인 |
\`\`\`

---

> 💡 **팁**: 커맨드 없이 그냥 말해도 됩니다. "이거 고쳐줘", "분석해줘" 같은 트리거 단어만 있으면 알아서 워크플로우를 실행합니다.

> 🐸 브라우저가 안 열릴 때 대표님이 "lockfile 지워"라고 해주시면 그때 삭제하는 게 안전합니다.
`;

const encoded = encodeURIComponent(NOTE_PATH);
const url = `${API_URL}/vault/${encoded}`;

const res = await fetch(url, {
  method: 'PUT',
  headers: {
    Authorization: `Bearer ${API_KEY}`,
    'Content-Type': 'text/markdown',
  },
  body: newContent,
});

if (res.ok) {
  console.log('✅ 뇽죵이 설명서 업데이트 완료 (' + res.status + ')');
} else {
  const body = await res.text().catch(() => '');
  console.error('❌ 업데이트 실패: ' + res.status + ' ' + res.statusText + '\n' + body);
  process.exit(1);
}
